## web (Next.js)

This is a minimal Next.js app. Install dependencies:

```bash
cd web
npm install
npm run dev
```

The app includes an API proxy `/api/proxy/ads` that forwards to the backend API (NEXT_PUBLIC_API_URL).
